# pnpm configuration for Sora Auto Queue Prompts

# Use shamefully-hoist to avoid dependency resolution issues with sharp and other native modules
shamefully-hoist=true

# Ensure native modules are properly built for the current platform
auto-install-peers=true

# Disable frozen lockfile for initial setup (can be enabled in CI)
# frozen-lockfile=false
